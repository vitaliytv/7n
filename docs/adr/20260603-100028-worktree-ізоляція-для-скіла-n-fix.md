---
session: 8bafb2ff-cee4-454a-92c6-ad9511f08553
captured: 2026-06-03T10:00:28+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-vitaliytv-7n/8bafb2ff-cee4-454a-92c6-ad9511f08553.jsonl
---

## ADR Worktree-ізоляція для скіла `n-fix`

## Context and Problem Statement
Скіл `n-fix` виконує діагностику та виправлення проєкту відповідно до правил у `.cursor/rules/`. Щоб уникнути побічних ефектів у основному робочому дереві під час автоматичних правок, скіл вимагає запуску в ізольованому git-worktree.

## Considered Options
* Запускати `n-fix` безпосередньо в основному дереві (`main`)
* Запускати `n-fix` виключно в окремому git-worktree (`.worktrees/<branch>-fix/`)

## Decision Outcome
Chosen option: "Запускати `n-fix` виключно в окремому git-worktree", because у `SKILL.md` зазначено `worktree: true` і є явна директива: «Виконується **виключно** в окремому git-worktree».

### Consequences
* Good, because transcript фіксує очікувану користь: preflight-перевірка (`git rev-parse --show-toplevel`) автоматично виявила, що агент запущений у `main`, і без запиту до користувача створила worktree `main-fix` через `npx @nitra/cursor worktree add "main-fix"`.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Preflight-команда: `git rev-parse --show-toplevel && git branch --show-current`
- Worktree створено командою: `npx @nitra/cursor worktree add "main-fix" "n-fix: worktree-only skill"`
- Шлях worktree: `/Users/vitalii/www/vitaliytv/7n/.worktrees/main-fix`
- Діагностика запускалася командою: `npx @nitra/cursor fix`
- Перевірені артефакти: `.claude/hooks/capture-decisions.sh`, `.claude/hooks/normalize-decisions.sh` — обидва збігаються з канонічними
- Результат: всі перевірки пройшли (`✅`), робоче дерево чисте
