---
session: 3bffbb6e-9a30-4d93-ac56-53ae96e21ce7
captured: 2026-06-09T09:03:14+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-vitaliytv-7n/3bffbb6e-9a30-4d93-ac56-53ae96e21ce7.jsonl
---

all 24 tests pass · zsh syntax OK · default-on verified · change-file created.
[assistant]
[thinking]

[assistant]
Зроблено. Тепер таймлайн **ввімкнено за замовчуванням** — щоб вимкнути, треба явно:
```
N7COMMIT_DEBUG=0 npx @7n/n push
```

Всі 24 тести проходять, zsh-синтаксис чистий, change-файл `npm/.changes/260609-0902.md` створено.

---

Transcript summary:

The transcript covers implementing a debug-timeline feature for the `n push` command (`npm/push.js`). The user asked why `npx @7n/n push` hangs at the `pi -p` LLM call, then requested full diagnostic logging of each stage (which tool, args, duration, exit code, response size and first lines). The assistant added `_n7dbg` / `_n7dbg_agent_done` helpers gated on `N7COMMIT_DEBUG`, instrumented all stages (fetch, add, context build, each LLM agent), added 3 tests, updated README and JSDoc. The user then requested the debug mode be on by default (opt-out via `N7COMMIT_DEBUG=0` instead of opt-in via `=1`). The gate string was flipped from `:-0`/`="1"` to `:-1`/`!="0"`, comments/JSDoc/README updated, tests updated, smoke-tested.

## ADR Debug-таймлайн `push`: ввімкнений за замовчуванням

## Context and Problem Statement
Команда `npx @7n/n push` «зависала» на невизначений час під час генерації commit-меседжу LLM-агентом (`pi -p`). Не було жодного способу зрозуміти, на якому з етапів (fetch, add, збір контексту, виклик `pi`/`claude`/`cursor-agent`) виникає затримка, тому що жодних проміжних таймінгових рядків у виводі не було.

## Considered Options
* Додати debug-таймлайн як `opt-in` (ввімкнений лише явним `N7COMMIT_DEBUG=1`)
* Додати debug-таймлайн як `opt-out` (увімкнений за замовчуванням, вимикається `N7COMMIT_DEBUG=0`)

## Decision Outcome
Chosen option: "debug-таймлайн як `opt-out`", because після реалізації opt-in-варіанту користувач явно запросив зробити діагностику активною за замовчуванням (`"активуй його за замовчуванням"`).

### Consequences
* Good, because transcript фіксує очікувану користь: без будь-якого env у stderr одразу видно 10 рядків таймлайну з тривалістю кожного етапу та кожного LLM-агента — не потрібно пам'ятати флаг при кожному відлагодженні.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Файли: `npm/push.js`, `npm/tests/push.test.mjs`, `npm/README.md`, `npm/.changes/260609-0902.md`
- Gate-рядок у zsh-скрипті: `[[ "${N7COMMIT_DEBUG:-1}" != "0" ]] || return 0` (обидва хелпери — `_n7dbg` і `_n7dbg_agent_done`)
- Вивід іде у **stderr** (`>&2`), щоб не потрапити в commit-меседж, який збирається з stdout
- Smoke-тест: без `env` — 10 рядків `🔎`; з `N7COMMIT_DEBUG=0` — 0 рядків
- Команди верифікації: `zsh -n /tmp/push_check.zsh`, `npx vitest run tests/push.test.mjs`
