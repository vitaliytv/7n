---
session: ab3011c1-78a9-4cd9-a140-83733702e73e
captured: 2026-06-03T06:49:16+03:00
transcript: /Users/vitaliytv/.claude/projects/-Users-vitaliytv-www-vitaliytv-7n/ab3011c1-78a9-4cd9-a140-83733702e73e.jsonl
---

Please provide the transcript for analysis. The message contained only partial transcript data — the section after "filename: 7n-n-0.1.0.tgz" appears cut off.

Based on the transcript provided, here are the durable decision records:

---

## ADR Назва bin-файлу: `n.js` замість числового `7.js`

## Context and Problem Statement
Початковий bin-файл у `npm/bin/` мав назву `7.js`. Під час розбору причини `npm warn publish "bin[7]" script name bin/7.js was invalid and removed` постало питання про придатність числового basename у значенні поля `bin` в `package.json`.

## Considered Options
* Залишити `bin/7.js`, змінивши лише ключ команди
* Перейменувати на `bin/cli.js`
* Перейменувати на `bin/n.js` (фінальний вибір)

## Decision Outcome
Chosen option: "перейменувати на `bin/n.js`", because користувач спочатку обрав `cli.js`, а потім переглянув рішення й вирішив назвати файл `n.js` (відповідно до кінцевого імені команди `n`), зберігаючи єдиний стиль між файлом і командою.

### Consequences
* Good, because назва файлу (`n.js`) і ключ команди (`"n"`) збігаються — полегшує навігацію та очевидність призначення файлу.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Файли: `npm/bin/n.js` (перейменовано з `bin/7.js` → `bin/cli.js` → `bin/n.js`), `npm/package.json`
- Відповідний `scripts.start` також оновлено: `"bun ./bin/n.js"`

---

## ADR Видалення префікса `./` зі значення поля `bin` в `package.json`

## Context and Problem Statement
При виконанні `npm publish` з'являлось попередження `npm warn publish "bin[7n]" script name bin/cli.js was invalid and removed` навіть після зміни ключа команди й перейменування файлу. Була потрібна точна причина, чому npm видаляє bin-запис під час публікації.

## Considered Options
* Змінити ключ команди (початкова гіпотеза — цифра у ключі)
* Перейменувати bin-файл (гіпотеза — basename без розширення = цифра)
* Видалити префікс `./` зі значення поля `bin`

## Decision Outcome
Chosen option: "видалити префікс `./` зі значення поля `bin`", because емпірична перевірка в ізольованому temp-пакеті показала, що навіть ключ `"cli"` з `"./bin/cli.js"` видаляється, тоді як `"7n"` з `"bin/cli.js"` (без `./`) проходить clean. `npm pkg fix` цю проблему не виявляє і не виправляє.

### Consequences
* Good, because transcript фіксує очікувану користь: `npm publish --dry-run` більше не видає попередження `auto-corrected / removed` після прибирання `./`.
* Bad, because `npm pkg fix` не ловить цю проблему — при повторному редагуванні `package.json` вручну `./` може повернутись непомітно.

## More Information
- Файл: `npm/package.json`, поле `bin`
- До: `"7n": "./bin/cli.js"` → після: `"7n": "bin/cli.js"` (пізніше `"n": "bin/n.js"`)
- Верифіковано командою `npm publish --dry-run` на temp-пакетах у `mktemp -d`

---

## ADR Ім'я npm-пакета: `@7n/n` замість `@7n/7`

## Context and Problem Statement
Спроба опублікувати пакет під назвою `@7n/7` завершилась помилкою `npm error 400 Bad Request "That word is not allowed"` — npm відхилив ім'я пакета через серверний денлист. Потрібна валідна публічна назва під скоупом `@7n`.

## Considered Options
* `@7n/7` — відхилено денлистом npm (E400)
* `@7n/cli` — вільне, перевірено через `npm view`
* `@7n/n` — вільне, перевірено через `npm view`; фінальний вибір

## Decision Outcome
Chosen option: "`@7n/n`", because користувач після перебору варіантів (`@7n/cli` → `@7n/n`) вирішив, що одна буква `n` є оптимальним коротким іменем, симетричним до скоупу `@7n` та відповідним до команди `n`.

### Consequences
* Good, because `npm publish` пройшов без помилок, пакет `@7n/n@0.1.0` опублікований у npmjs.com.
* Bad, because ім'я пакета (`@7n/n`) і команда, яку встановлює `bin` (`n`), збігаються — це може дезорієнтувати якщо в майбутньому скоуп або bin-команда зміняться окремо.

## More Information
- Файл: `npm/package.json`, поле `name`
- Перевірка доступності: `npm view @7n/n` → `E404 Not Found` (вільне перед публікацією)
- Організація `7n` у npmjs.com належить користувачу `vitaliytv`, тому скоуп `@7n` валідний
- `publishConfig.access: "public"` додано для уникнення `E402 Payment Required` (scoped-пакети за замовчуванням private)

---

## ADR Додавання `publishConfig.access: "public"` до `package.json`

## Context and Problem Statement
Спроба `npm publish` для scoped-пакета (`@7n/...`) завершилась помилкою `npm error 402 Payment Required — You must sign up for private packages`. Npm за замовчуванням вважає scoped-пакети приватними.

## Considered Options
* Передавати `--access public` при кожному виклику `npm publish`
* Додати `publishConfig.access: "public"` до `package.json` (постійно)

## Decision Outcome
Chosen option: "додати `publishConfig.access: \"public\"` до `package.json`", because закріплення в `package.json` гарантує публічний доступ при будь-якому виклику `npm publish` (у тому числі в CI) без необхідності пам'ятати флаг.

### Consequences
* Good, because `npm publish --dry-run` підтверджує `with tag latest and public access` — флаг більше не потрібен.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Файл: `npm/package.json`, поле `publishConfig`
- Значення: `{ "access": "public" }`
- Верифіковано: `npm pkg get publishConfig` → `{ "@7n/n": { "access": "public" } }`
