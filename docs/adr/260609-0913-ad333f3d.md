---
session: ad333f3d-ec6d-4010-bb52-5a2c0641f4e4
captured: 2026-06-09T09:13:17+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-vitaliytv-7n/ad333f3d-ec6d-4010-bb52-5a2c0641f4e4.jsonl
---

Не маю доступу до файлів за межами воркдиру сесії. Але з транскрипту маю всі необхідні рядки — готую точний патч-інструкцію:

---

## Патч для `nitra/cursor` — 4 файли, 1 рядок у кожному

### Причина

`node npm/bin/n-cursor.js release` — шлях, що існує **лише** у `nitra/cursor` (де файл лежить у `npm/bin/`). Для будь-якого споживача (7n і т.д.) цей шлях відсутній → `MODULE_NOT_FOUND`. Портабельна форма — `n-cursor release` (бінарник у `node_modules/.bin/` після `bun install`).

---

### Файл 1 — сніпет-джерело істини

**`npm/rules/npm-module/policy/npm_publish_yml/template/npm-publish.yml.snippet.yml`**, крок `release` (~рядок 40):

```yaml
# було:
run: node npm/bin/n-cursor.js release
# стає:
run: n-cursor release
```

---

### Файл 2 — prose у правилі (source)

**`npm/rules/npm-module/npm-module.mdc`**, рядок ~68:

```
# було:
node npm/bin/n-cursor.js release
# стає:
n-cursor release
```

---

### Файл 3 — synced копія правила

**`.cursor/rules/n-npm-module.mdc`**, той самий рядок ~68 (синхрокопія):

```
# було:
node npm/bin/n-cursor.js release
# стає:
n-cursor release
```

---

### Файл 4 — власний workflow `nitra/cursor`

**`.github/workflows/npm-publish.yml`**, рядок 40:

```yaml
# було:
run: node npm/bin/n-cursor.js release
# стає:
run: n-cursor release
```

---

### Після правки в `nitra/cursor` → 7n

Коли сніпет оновлено і `/n-fix` у `nitra/cursor` опублікує нову версію правила, у `7n` достатньо:

```yaml
# .github/workflows/npm-publish.yml:40
run: n-cursor release
```

Це вже збіжиться зі сніпетом — conformance-перевірка пройде, і workflow публікації запрацює.
