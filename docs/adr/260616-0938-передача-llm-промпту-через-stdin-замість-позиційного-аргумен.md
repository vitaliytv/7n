---
session: 10799558-6237-4779-978f-51c2d521f437
captured: 2026-06-16T09:38:20+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-vitaliytv-7n/10799558-6237-4779-978f-51c2d521f437.jsonl
---

Git commit is blocked by your current permission settings. Here are the exact commands to run manually:

```bash
cd /Users/vitalii/www/vitaliytv/7n

git commit -m "release: @7n/n@0.12.1 [skip ci]

Ручна синхронізація git із npm-registry: CI опублікував 0.12.1 (stdin-фікс push),
але commit-back відхилено (non-fast-forward через паралельний коміт fb0797d)."

git push origin main
```

**Що вже staged і готово:**
- `npm/package.json`: `0.12.0` → `0.12.1`
- `npm/CHANGELOG.md`: додано запис `0.12.1` з описом stdin-фіксу
- `npm/.changes/260616-0922.md`: видалено (сконсумовано у release)

**Чому `[skip ci]`:** без нього після пушу CI знову запустить `n-cursor release` — знайде нема `.changes`-файлів, job впаде. З `[skip ci]` GitHub Actions ігнорує цей коміт і не тригерить pipeline.

Після цього `git log`, npm registry і локальний репозиторій всі будуть показувати `0.12.1`.

Now let me produce the ADR that was requested from the transcript.

---

## ADR Передача LLM-промпту через stdin замість позиційного аргументу

## Context and Problem Statement
`n7push_gen_message` передавала зібраний diff-контекст (до 5.4 MB) LLM-агентам (`pi`, `claude`, `cursor-agent`) як позиційний аргумент у командному рядку. macOS обмежує сумарний розмір `argv` константою `ARG_MAX` = 1 MiB; при перевищенні `execve` повертає `E2BIG`, zsh виводить `argument list too long: pi`, а всі три агенти завершуються з rc=127 ще до старту — commit-меседж не генерується і `push` аварійно завершується.

## Considered Options
* Передавати prompt через stdin (тимчасовий файл + `< "$pf"`)
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Передавати prompt через stdin (тимчасовий файл + `< "$pf"`)", because stdin не має обмежень `ARG_MAX`, і всі три агенти (`pi -p`, `claude -p`, `cursor-agent -p`) підтверджено читають prompt зі stdin у `-p`-режимі (перевірено в transcript).

### Consequences
* Good, because transcript фіксує очікувану користь: 3.7 MB prompt більше не упирається в ARG_MAX; всі три агенти запускаються коректно (rc=0 при тестуванні stdin-методу).
* Good, because як додаткова міра — diff-контекст тепер обрізається тришарово: за рядками (`N7COMMIT_MAX_DIFF_LINES`), за довжиною рядка (`N7COMMIT_MAX_LINE`, дефолт 500 символів) і за байтами (`N7COMMIT_MAX_DIFF_BYTES`, дефолт 256 KiB) — один мінімізований/base64-рядок більше не роздуває prompt.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- Файл змін: `npm/push.js`, функція `_n7push_gen_message` (~рядки 66–145)
- Тести: `npm/tests/push.test.mjs` — додано 2 нові тести (stdin-механіка + байтове обрізання); всього 27/27 pass
- Change-файл: `npm/.changes/260616-0922.md` (`bump: patch`, `section: Fixed`)
- Діагноз: `ARG_MAX` = 1 048 576 байт (`getconf ARG_MAX`); prompt ~3.7 MB → `E2BIG` → rc=127
- Команда перевірки агентів: `echo "test" | pi -p --no-session --no-context-files --no-tools`
- Env-змінні для налаштування: `N7COMMIT_MAX_DIFF_LINES`, `N7COMMIT_MAX_LINE`, `N7COMMIT_MAX_DIFF_BYTES`
