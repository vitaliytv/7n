---
bump: patch
section: Fixed
---
push: commit-меседж генерується навіть на дуже великих diff — prompt передається LLM-агентам (pi/claude/cursor-agent) через STDIN, а не позиційним аргументом, тож мегабайтний контекст більше не впирається в ARG_MAX ОС («argument list too long», E2BIG/rc=127). Додатково diff-контекст обрізається не лише за рядками (N7COMMIT_MAX_DIFF_LINES), а й за довжиною рядка (N7COMMIT_MAX_LINE, дефолт 500) та сумарними байтами (N7COMMIT_MAX_DIFF_BYTES, дефолт 256 KiB) — один мініфікований/згенерований рядок більше не роздуває контекст.
