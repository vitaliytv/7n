---
bump: minor
section: Changed
---
merge omlx Tier-3: валідатор V8 — для *.json увесь файл після резолву має парситись (JSON.parse); якщо ні (типово модель губить кому) — пере-резолв із підсилювальним правилом, після ретраїв лишаються маркери замість тихо-зламаного JSON. Резолвер передає isJson за розширенням; промпт хунка приймає додаткове правило
